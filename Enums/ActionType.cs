namespace ConfluencePrototype.Enums
{
    public enum ActionType
    {
        Draw,
        Execute,
        Install,
        InstallInterrupt,
        Move,
        Play,
        Terminate,
        Trash,
    }
}
